# The Node worker (server/design-agent/openhands-handler.js) passes these
# through to design_task.py as env vars — it does not read this file
# directly. LLM_API_KEY is the existing OPENAI_API_KEY already used
# elsewhere in this repo (REPORT_PROVIDER=openai) — no new secret needed.
LLM_MODEL=gpt-4o-mini
LLM_API_KEY=
# LLM_BASE_URL=
